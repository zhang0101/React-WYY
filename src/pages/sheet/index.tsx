import React, { Component } from "react";
import { withRouter, RouteComponentProps } from "react-router-dom";

class Sheet extends Component<RouteComponentProps> {
  render() {
    return <div></div>;
  }
}

export default withRouter(Sheet);
